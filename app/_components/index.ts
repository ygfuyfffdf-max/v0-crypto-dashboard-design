// 🚀 CHRONOS COMPONENTS INDEX - CHRONOS INFINITY
// Archivo de barril para exportar componentes existentes

// Authentication Components
export { default as KocmokAnimatedLogo } from './auth/KocmokAnimatedLogo';
export { default as WelcomeDashboard } from './auth/WelcomeDashboard';

// Admin Components (only existing files)
export { default as UserCreationWizard } from './admin/UserCreationWizard';

// UI Components (only existing files)
export { Badge } from './ui/badge';
export { Button } from './ui/button';
export { Card } from './ui/card';
export { Input } from './ui/input';
export { Progress } from './ui/progress';
export { Select } from './ui/select';
export { Tabs, TabsContent, TabsList, TabsTrigger } from './ui/tabs';
// label, slider, switch, dialog, table — modules not found, removed

// Types and Constants — removed (modules not found at _lib/types and _lib/constants)